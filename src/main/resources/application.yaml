server:
  port: 8181

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    properties.mail.smtp:
      auth: true
      starttls:
        enable: true
        required: true
    test-connection: true